import React, { FC } from "react";
import Layout from "../compenents/Layout";
import Banner from "../compenents/Banner";

const Home: FC = () => {
    return (
        <>
            <Layout />
            <Banner />
        </>
    );
};

export default Home;
